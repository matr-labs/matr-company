---
import BaseLayout from "@layouts/BaseLayout.astro";
import Container from "@ui/Container.astro";
import Section from "@ui/Section.astro";
import Heading from "@ui/Heading.astro";
import Text from "@ui/Text.astro";
import ImageBlock from "@ui/ImageBlock.astro";
import Artists from "@sections/Artists.astro";
import Manifesto from "@sections/Manifesto.astro";
import Services from "@sections/Services.astro";
import CTA from "@sections/CTA.astro";
import { home } from "@content/home";
import H3 from "@ui/Heading.astro";

const { hero, intro, industries, cta } = home;
---

<BaseLayout seo={{ title: "Home", titleAbsolute: true }}>

  <section
    data-theme="dark"
    class="sticky top-8 z-0 h-[88svh] overflow-hidden bg-bg text-text sm:h-[90svh] lg:h-svh"
  >
  <div class="hero-parallax absolute inset-0 will-change-transform">
    
    <ImageBlock
      src={hero.image}
      alt={hero.imageAlt}
      loading="eager"
      sizes="100vw"
      class="hero-img absolute inset-0 h-full w-full will-change-transform"
    />



    <div
      class="absolute inset-0 bg-[linear-gradient(to_top,rgba(0,0,0,0.85),rgba(0,0,0,0.4)_45%,rgba(0,0,0,0.2))]"
      aria-hidden="true"
    >
    </div>


    </div>


  </section>

  
  <div class="relative z-10">
     <div class="text-white flex justify-center w-full absolute flex-col -top-[88vh] text-center gap-[60vh]">
    <H3>{hero.brand}</H3>
    </div>
  <Section width="full" tone="surface">
    <div class="relative flex flex-1 flex-col justify-end gap-12">
      
      <div class="hero-rise flex flex-col gap-6">
        <Heading level={1} size="hero" tracking="tighter">
         {intro.heading}
        </Heading>
        <H3>{intro.body}</H3>
      </div>


      

<ul class="grid grid-cols-4 gap-6 sm:gap-8 lg:gap-16 overflow-hidden lg:grid-cols-8 mt-8">
    {
      industries.items.map((item) => (
        <li class="flex aspect-[2/1] items-center justify-center bg-surface ">
          {item.href ? (
            <a
              href={item.href}
              class="flex h-full w-full items-center justify-center opacity-50 transition-opacity hover:opacity-100"
              aria-label={item.name}
            >
              <ImageBlock
                src={item.logo}
                alt={item.name}
                class="max-h-12 w-auto object-contain grayscale"
              />
            </a>
          ) : (
            <ImageBlock
              src={item.logo}
              alt={item.name}
              class="max-h-12 w-auto object-contain opacity-50 grayscale"
            />
          )}
        </li>
      ))
    }
  </ul>

    </div>

  </Section>


  <Artists />

  <Manifesto />

  <Services />

  <CTA title={cta.title} body={cta.body} action={cta.action} />

  </div>
</BaseLayout>

<style>
  
  .hero-img {
    transform: scale(1.25);
    transform-origin: center;
  }

  @media (prefers-reduced-motion: reduce) {
    .hero-img {
      transform: none !important;
    }
  }

  .hero-rise {
    animation: hero-rise 0.8s cubic-bezier(0.16, 1, 0.3, 1) both;
  }

  @keyframes hero-rise {
    from {
      opacity: 0;
      transform: translateY(1.5rem);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .scroll-dot {
    animation: scroll-pulse 1.8s ease-in-out infinite;
  }

  @keyframes scroll-pulse {
    0%,
    100% {
      opacity: 1;
      transform: scale(1);
    }
    50% {
      opacity: 0.4;
      transform: scale(0.7);
    }
  }
</style>

<script>

  const FACTOR = 0.1;
  let ticking = false;

  const prefersReduce = () =>
    window.matchMedia("(prefers-reduced-motion: reduce)").matches;

  function render() {
    ticking = false;

    const layer = document.querySelector<HTMLElement>(".hero-parallax");
    if (!layer) return;
    if (prefersReduce()) {
      layer.style.transform = "";
      return;
    }
    const shift = Math.max(0, window.scrollY) * FACTOR;
    layer.style.transform = `translate3d(0, ${-shift}px, 0)`;
  }

  function onScroll() {
    if (!ticking) {
      ticking = true;
      requestAnimationFrame(render);
    }
  }

  window.addEventListener("scroll", onScroll, { passive: true });

  document.addEventListener("astro:page-load", render);
</script>
